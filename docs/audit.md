# Audit of the build brief

One line per claim that matters. Status: **confirmed**, **corrected**, or
**could not verify**, each with the evidence. Sources are files in this
repository (`firmware/` = crosspoint-reader checkout at 7db14a01 with
freeink-sdk at cb9167d; `qemu/` = espressif/qemu `esp-develop` at febae182;
`docs/device/idf-headers/` = ESP-IDF `release/v5.5` headers fetched 2026-09-06)
or measurements on the desk unit recorded in `docs/device/`.

## Environment and repositories

| Claim | Status | Evidence |
|---|---|---|
| Espressif QEMU `esp-develop` head is `febae182` (2026-04-29) | confirmed | `git ls-remote` shows `febae182e132…` for `refs/heads/esp-develop` |
| Latest Espressif tag is `v10.1.2`; pin the fork branch to it | **corrected** | `v10.1.2` in espressif/qemu is the plain upstream QEMU release tag (commit `ccaea6b`, "Update version for 10.1.2 release"). It has **no** `hw/xtensa/esp32s3.c`, no `esp32*` machines. The Espressif machines live only on `esp-develop` (tags `esp-develop-<ver>-<date>`). The fork branch `x4pro` is based on `esp-develop` @ febae182 instead. |
| `qemu-system-xtensa -machine esp32s3 -nographic` prints the ROM banner | confirmed | With no flash drive: `ESP-ROM:esp32s3-20210327`, `rst:0x1 (POWERON),boot:0x4 (SPI_FLASH_BOOT)`, then a LoadStoreError at 0x3c800000 (flash read with no MTD), which is expected. |
| Configure line from the README works with SDL/GTK off | confirmed (macOS) | Built with `--target-list=xtensa-softmmu --enable-gcrypt --enable-slirp --enable-png --disable-user --disable-capstone --disable-vnc --disable-gtk --disable-sdl`. Two host deviations: QEMU refuses a source path with spaces (repo moved to `/Users/mini/x4pro-emu`); on macOS the binary is produced as `qemu-system-xtensa-unsigned` and `ninja qemu-system-xtensa` runs the entitlement step to produce the signed one. |
| `screendump` supports `format: png` when built with libpng | confirmed | `qapi/ui.json` `ImageFormat` = `['ppm','png']`; `build/config-host.h` has `CONFIG_PNG`. |
| qdev `DEFINE_PROP_*` properties are read-only after realize | confirmed | `hw/core/qdev-properties.c:29-40` `qdev_prop_allow_set()` rejects sets when `dev->realized && !info->realized_set_allowed`. Runtime controls must be `object_property_add` visitors. |
| `hw/sd/sd.c` refuses SD images whose size is not a power of 2 | confirmed | `hw/sd/sd.c` `sd_realize`: "Invalid SD card size … must be a power of 2" (grep `power of 2`). |
| `dwc_sdmmc` interrupt wiring pattern | confirmed | `hw/xtensa/esp32s3.c:430` `sysbus_connect_irq(SYS_BUS_DEVICE(&s->sdmmc), 0, qdev_get_gpio_in(intmatrix_dev, ETS_SDIO_HOST_INTR_SOURCE))`; `dwc_sdmmc.c:222` `dwc_sdmmc_update_irq` raises `s->irq` from `rintsts & intmask` plus IDMAC status. |
| Line counts: esp32_gpio.c 103, esp32c3_jtag.c 82, esp32s3_rtc_cntl.c 245, esp32_spi.c 366, esp32_i2c.c 283, esp32_ledc.c 154 | confirmed | `wc -l` on `esp-develop` @ febae182: 103 / 82 / 245 / 366 / 283 / 154 (esp32s3_gpio.c 49, dwc_sdmmc.c 592, esp32s3.c 1002). |
| GPIO: `esp32s3_gpio.c` inherits `TYPE_ESP32_GPIO`, which implements only the strap register | confirmed | `esp32_gpio.c` read handles only `A_GPIO_STRAP`; write is empty. |
| USB Serial/JTAG: `esp32c3_jtag.c` is a pure stub (reads 0, writes discarded) | confirmed | `esp32c3_jtag_read` returns 0, `esp32c3_jtag_write` is empty; region size 0x84. |
| RTC_CNTL model tracks reset causes only; sleep entry does nothing so `esp_deep_sleep_start()` "falls through into its abort" | **corrected (detail)** | The model handles OPTIONS0, TIME_UPDATE/TIME0/1, RESET_STATE, STORE0-7, CLK_CONF, SW_CPU_STALL; everything else reads 0 and writes are dropped. Sleep entry in ESP-IDF (`rtc_sleep_start`) sets `RTC_CNTL_STATE0.SLEEP_EN` and then spins on `RTC_CNTL_INT_RAW_REG & (SLP_REJECT|SLP_WAKEUP)`; with both reading 0 the CPU **spins forever** rather than aborting. The sleep hook must set `SLP_WAKEUP_INT_RAW` (bit 0 of +0x44) or reset the machine. |
| Deep-sleep bit is in `RTC_CNTL_STATE0_REG` | confirmed | `rtc_cntl_reg.h`: `RTC_CNTL_STATE0_REG = base+0x18`, `RTC_CNTL_SLEEP_EN = BIT(31)`, `SLP_REJECT = BIT(30)`, `SLP_WAKEUP = BIT(29)`. Reset cause 5 = `ESP32_DEEPSLEEP_RESET` in `esp32s3_rtc_cntl.h`. `EXT_WAKEUP1_STATUS_REG = +0xE4`, `SLP_WAKEUP_CAUSE_REG = +0x130`. |
| Espressif README: flash sizes 2/4/8/16 MB, `-m` for PSRAM, octal via `-global driver=ssi_psram,property=is_octal,value=true`, efuse 1 KB file via `-drive …,id=efuse` + `-global driver=nvram.esp32s3.efuse,property=drive,value=efuse`, strap via `esp32s3.gpio.strap_mode`, WDT switch `timer.esp32c3.timg.wdt_disable` | **corrected (WDT)** | `docs/device/ref/qemu-esp32s3-README.md` (fetched from espressif/esp-toolchain-docs). Note the README lives in esp-toolchain-docs, not in the qemu tree. The watchdog switch is wrong for the S3 in this tree: `-global driver=timer.esp32c3.timg,…` prints "invalid class name"; the S3 timer group is `timer.esp32s3.timg` (`include/hw/timer/esp32s3_timg.h`), which inherits the `wdt_disable` property from `hw/timer/esp_timg.c`. |
| eFuse file is 1 KB of zeros; fields replayed via `espefuse.py --port socket://` | confirmed, plus a shortcut | `hw/nvram/esp_efuse.c:134` `blk_pread(s->blk, 0, size, &s->efuses.blocks, 0)` reads the block struct (BLOCK0 6 words, BLOCK1 6, BLOCK2..10 8 each) from file offset 0. `tools/mkefuse.py` writes the real device's `espefuse dump` words straight into that layout; no socket burn needed. Verified: QEMU boot log shows the device MAC. |
| `-icount` on the two-CPU machine | could not verify yet | To be tested in M1/M3 when determinism matters. |
| xtensa gdb binary name in the PlatformIO toolchain | **corrected** | It is `xtensa-esp-elf-gdb` (multi-target, gdb 17.1), shipped as `~/.platformio/packages/tool-xtensa-esp-elf-gdb/bin/xtensa-esp-elf-gdb-3.9` (python-version suffix; a `-3.8` twin exists). There is no `xtensa-esp32s3-elf-gdb`. Use `set arch xtensa` / `target remote :1234`. |
| esptool spells the merge command `merge_bin`, newer `merge-bin` | confirmed | esptool 5.4.0 installed: `merge-bin`, `read-flash`, `chip-id`, `image-info`; underscore forms print a deprecation warning. There is no `partition-table` subcommand in 5.4.0; `tools/` parses the table itself. |
| S3 bootloader offset is 0x0 | confirmed | Device dump: image header 0xE9 at 0x0, `image-info` = bootloader v1, ESP-IDF v6.0.1, entry 0x403c8870. |
| `boot_app0.bin` lives in the pioarduino Arduino framework package under `tools/partitions/` | confirmed | `~/.platformio/packages/framework-arduinoespressif32/tools/partitions/boot_app0.bin` (8 KB, otadata selecting ota_0). Written at 0xE000 by `tools/mkflash.py`. |
| pioarduino platform pinned at 55.03.311 | confirmed | `firmware/platformio.ini` `[base] platform = …/55.03.311/platform-espressif32.zip`; CrossPoint version 1.6.0. It unpacks Arduino core **3.3.11** (`framework-arduinoespressif32`), prebuilt ESP-IDF **5.5.5** libs+headers (`framework-arduinoespressif32-libs` 5.5.5+sha.b774170ff46), `toolchain-xtensa-esp-elf` 14.2.0, `tool-xtensa-esp-elf-gdb` 17.1.0, `tool-esptoolpy` 5.3.0. The fetched v5.5 `gpio_reg.h` is byte-identical to the one in that package. |
| Arduino core's `SPI.writeBytes` pushes 64-byte chunks through W0..W15 | confirmed (core 3.3.11) | `esp32-hal-spi.c` `spiWriteNL` (line 1511 in 3.3.11): `c_len = (len > 64) ? 64 : len`, 16 longs into `data_buf[]`, `mosi_dlen = c_len*8-1`, `cmd.update=1`, spin on `update`, `cmd.usr=1`, spin on `usr`. |
| HWCDC `connected` semantics (IN_EMPTY / OUT_RECV_PKT set it, BUS_RESET clears it; SOF tick hook; `tx_timeout_ms`) | confirmed (core 3.3.11) | `HWCDC.cpp`: `connected = true` on `SERIAL_IN_EMPTY` (when the ring buffer path completes) and on `SERIAL_OUT_RECV_PKT`; `false` on `USB_BUS_RESET` and after `tx_timeout_ms` of failed sends; `isPlugged()` is ESP-IDF's `usb_serial_jtag_is_connected()` (tick hook: reads INT_RAW, checks SOF, clears it, disconnect after `ALLOWED_NO_SOF_TICKS+1` ticks without SOF); `isCDC_Connected()` returns false while unplugged, and while plugged-but-not-connected it re-arms IN_EMPTY and sets `wr_done` so the next host IN token flips `connected`. The LL writes bytes to EP1 while `ep1_conf.serial_in_ep_data_free`, flushes with `ep1_conf.wr_done = 1`, reads EP1 while `serial_out_ep_data_avail`. CrossPoint calls `setTxTimeoutMs(1)` (`src/main.cpp` "load-bearing 1"). |

## Hardware facts (brief table vs. sources)

| Claim | Status | Evidence |
|---|---|---|
| SoC ESP32-S3R8, 16 MB flash, 8 MB octal PSRAM, memory type `dio_opi` | confirmed | `esptool chip-id`: ESP32-S3 (QFN56) rev v0.2, "Embedded PSRAM 8MB (AP_3v3)", 40 MHz crystal; efuse `PSRAM_CAP=8M`, `PSRAM_VENDOR=AP_3v3`; device boot log `octal_psram: vendor id 0x0d (AP) … density 0x03 (64 Mbit)`, `Found 8MB PSRAM`, `flash io: qio`; `platformio.ini` x4pro env `board_build.arduino.memory_type = dio_opi`. |
| Panel 800x480, SSD1677 early / UC8179 later, detected at boot | **corrected (incomplete)** | Three variants exist, not two: SSD1677, UC8179, and **UC8279** (800x480 X4 variant, `Uc8279X4Driver.cpp`). `XteinkDetect.cpp` promotes SSD1677 → UC8279 when VER byte 2 (LUT_VER) is 0x02/0x68/0x69, else → UC8179. The stock app on the desk unit contains all three driver classes (`strings`: `SSD1677_800x480`, `UC8179_800x480`, `UC8279_800x480`). The desk unit's factory NVS `hw_calib/screenType = 2`, which the SDK maps to **UC8279** (1/0x0B = UC8179, 2/0x0C = UC8279, else SSD1677). Final confirmation needs the `[XTDET]` line from a CrossPoint boot on the device. |
| Panel pins SCLK 12, MOSI 11, CS 13, DC 18, RST 14, BUSY 6 (busy = HIGH) | confirmed | `BoardConfig.h:1495` `XTEINK_X4_PRO.display = {12, 11, 13, 18, 14, 6, PIN_UNASSIGNED}`; support doc "Confirmed display pinout". |
| Stock clocks SPI at 5 MHz, CrossPoint at 20 MHz | confirmed | `BoardConfig.h` `displaySpiHz = 20000000` with the comment "OEM clocks the panel at only 5 MHz (SPISettings 0x4C4B40)". |
| Touch GT911 at 0x5D (alt 0x14), SDA 39 / SCL 38, 400 kHz, INT 10, RST 4 | confirmed | `BoardConfig.h` touch struct `{Gt911, 39, 38, 10, 4, 0x5D, …, 0x14, …}`. The support doc's I²C-map section still says "INT=GPIO4, RST=GPIO10" (stale); the touch section and the struct say INT=10/RST=4. |
| GT911 powered only while GPIO2 is LOW and GPIO1 HIGH | confirmed | `BoardConfig.h`: `powerEnable=2`, `powerEnableActiveHigh=false`; `power.latch0 = {1}`; support doc "Power rails". |
| Touch mounted portrait: X 0..480, Y 0..800; profile sets swapXY and flipY | confirmed | struct: `rawMaxX=799, rawMaxY=479, swapXY=true, flipX=false, flipY=true` ("confirmed by corner-tap"). The support doc's "flipX/flipY pending" is older than the struct. |
| Home pad is bit 4 of 0x814E | confirmed | `InputManager.cpp:2080` `homeKeyDown = (status & 0x10)`. |
| Buttons Left GPIO0, Right GPIO7, Power GPIO3, active-low, pull-ups, GPIO0 is a strap | confirmed | struct `{…, up=0, down=7, power=3, powerActiveHigh=false}`; support doc. |
| SDMMC 1-bit slot 1: CLK 41, CMD 42, DAT0 40, 40 MHz; gated by GPIO5 active-low; pulse HIGH 80 ms / LOW 120 ms, init + sector-0 read, retry | confirmed | struct `sdmmc = {41, 42, 40, -, -, -, 1}`, `sd.powerEnable = 5` active-low; support doc "Storage". |
| Frontlight cool GPIO8 ch4, warm GPIO9 ch5, 25 kHz, 10-bit, active-high | confirmed | struct `frontlight = {8, 25000, 10, true, 9}`; support doc says stock 7.0.8 passes 25 kHz/10-bit (an older dump used 10 kHz). |
| RTC BM8563 (PCF8563-compatible) at 0x51 | confirmed | struct `rtc = {39, 38, 400000, 0x51, …, RtcType::Pcf8563}`; stock app string `XTEink::BM8563Driver`. |
| CW2017 at 0x63; 0% until 80-byte BATINFO written to 0x10..0x5F; SoC reg 0x04; VCELL 0x02/0x03, mV = (raw*5+8)>>4 | confirmed (doc) / not yet in code | Support doc "Battery"; `BatteryMonitor` source not yet audited (M4). |
| Charger STAT on GPIO21, active-high | confirmed | struct `batteryChargeStatus = 21`, tail `batteryChargeStatusActiveHigh = true`. |
| USB native on GPIO19/20; console is USB Serial/JTAG; TinyUSB MSC only from the file-transfer screen | confirmed | Device enumerates as VID 0x303A PID 0x1001 "USB JTAG_serial debug unit" under stock at idle; `platformio.ini` x4pro comment: "USB Drive starts MSC only for that File Transfer activity". |
| Stock flash layout (nvs 0x9000, otadata 0xE000, app0 0x10000/0x7E0000, app1 0x7F0000/0x7E0000, spiffs 0xFD0000/0x14000, coredump 0xFE4000/0x1C000) | confirmed | Parsed from the dump at 0x8000 (`docs/device/partitions.md`). |
| The stock device boots app1; app0 is an older image for a different variant | **corrected for this unit** | otadata entry 0: seq=1, state=2 (VALID), entry 1 erased → boot slot `(1-1)%2 = 0` = **app0**. app0 = `xteink_app` 7.2.4 (IDF v6.0.1, built 2026-08-14); **app1 at 0x7F0000 is erased (0xFF)**. The device boot log confirms 7.2.4 runs. The support doc describes a different unit whose app0 was `ESP32S3_X4_TL`. Consequence: "the stock image stays in the other slot" is false here unless we copy it there first. |
| CrossPoint flash layout (app0 0x10000/0x640000, app1 0x650000/0x640000, spiffs 0xC90000/0x360000, coredump 0xFF0000/0x10000) | confirmed | `firmware/partitions.csv`. |
| Peripheral base addresses | confirmed | `reg_base.h`: GPIO 0x60004000, RTC_CNTL 0x60008000, RTC_IO 0x60008400, IO_MUX 0x60009000, I2C0 0x60013000, LEDC 0x60019000, SPI2 0x60024000, SPI3 0x60025000, SDMMC 0x60028000, USB_SERIAL_JTAG 0x60038000, USB_WRAP 0x60039000. |

## Register offsets (all from `docs/device/idf-headers/`, ESP-IDF v5.5)

| Block | Status | Offsets |
|---|---|---|
| GPIO | confirmed | BT_SELECT +0x00, OUT +0x04, OUT_W1TS +0x08, OUT_W1TC +0x0C, OUT1 +0x10, OUT1_W1TS +0x14, OUT1_W1TC +0x18, ENABLE +0x20, ENABLE_W1TS +0x24, ENABLE_W1TC +0x28, ENABLE1 +0x2C (W1TS/W1TC +0x30/+0x34), STRAP +0x38, IN +0x3C, IN1 +0x40, STATUS +0x44, STATUS_W1TS +0x48, STATUS_W1TC +0x4C, STATUS1 +0x50 (W1TS/W1TC +0x54/+0x58), PCPU_INT +0x5C, PCPU_NMI_INT +0x60, CPUSDIO_INT +0x64, PCPU_INT1 +0x68, PINn +0x74+4n (INT_TYPE bits 7..9, INT_ENA bits 13..17, PAD_DRIVER bit 2), STATUS_NEXT +0x14C, FUNCn_IN_SEL_CFG +0x154+4n, FUNCn_OUT_SEL_CFG +0x554+4n, CLOCK_GATE +0x62C. |
| SPI2 (GP-SPI) | confirmed | CMD +0x00 (USR bit 24, UPDATE bit 23), ADDR +0x04, CTRL +0x08, CLOCK +0x0C, USER +0x10 (USR_COMMAND 31, USR_ADDR 30, USR_DUMMY 29, USR_MISO 28, USR_MOSI 27, MOSI_HIGHPART 25, MISO_HIGHPART 24), USER1 +0x14, USER2 +0x18, MS_DLEN +0x1C (bits 0..17), MISC +0x20, DIN_MODE +0x24, DMA_CONF +0x30, DMA_INT_ENA/CLR/RAW/ST +0x34/+0x38/+0x3C/+0x40 (TRANS_DONE bit 12), W0..W15 +0x98..+0xD4, SLAVE +0xE0, CLK_GATE +0xE8, DATE +0xF0. |
| I2C0 | confirmed | SCL_LOW_PERIOD +0x00, CTR +0x04 (MS_MODE 4, TRANS_START 5, FSM_RST 10, CONF_UPGATE 11), SR +0x08 (RXFIFO_CNT bits 0..5, ARB_LOST 3, BUS_BUSY 4, TXFIFO_CNT bits 18..23), TO +0x0C, SLAVE_ADDR +0x10, FIFO_ST +0x14, FIFO_CONF +0x18, DATA +0x1C, INT_RAW/CLR/ENA/STATUS +0x20/+0x24/+0x28/+0x2C (END_DETECT 3, ARB_LOST 5, TRANS_COMPLETE 7, TIME_OUT 8, TRANS_START 9, NACK 10), SDA_HOLD +0x30 … CLK_CONF +0x54, COMD0..7 +0x58..+0x74 (byte_num 0..7, ack_en 8, ack_exp 9, ack_val 10, op_code 11..13, done 31; RSTART 6, WRITE 1, READ 3, STOP 2, END 4), TXFIFO_START +0x100, RXFIFO_START +0x180. |
| LEDC | confirmed | LSCHn_CONF0 +0x14n (TIMER_SEL bits 0..1, SIG_OUT_EN 2, PARA_UP 4), HPOINT +0x14n+4, DUTY +0x14n+8 (duty << 4), CONF1 +0x14n+0xC, DUTY_R +0x14n+0x10; LSTIMERn_CONF +0xA0+8n (DUTY_RES bits 0..3), INT_RAW/ST/ENA/CLR +0xC0..+0xCC, CONF +0xD0. Channels 4/5 → CONF0 +0x50/+0x64, DUTY +0x58/+0x6C. |
| USB_SERIAL_JTAG | confirmed | EP1 +0x00, EP1_CONF +0x04 (WR_DONE 0, IN_EP_DATA_FREE 1, OUT_EP_DATA_AVAIL 2), INT_RAW +0x08 / INT_ST +0x0C / INT_ENA +0x10 / INT_CLR +0x14 (JTAG_IN_FLUSH 0, SOF 1, SERIAL_OUT_RECV_PKT 2, SERIAL_IN_EMPTY 3, PID_ERR 4, CRC5 5, CRC16 6, STUFF 7, IN_TOKEN_REC_IN_EP1 8, USB_BUS_RESET 9, OUT_EP1_ZERO_PAYLOAD 10, OUT_EP2_ZERO_PAYLOAD 11), CONF0 +0x18, TEST +0x1C, JFIFO_ST +0x20, FRAM_NUM +0x24, IN_EP0_ST +0x28, IN_EP1_ST +0x2C, OUT_EP0_ST +0x38, OUT_EP1_ST +0x3C, MISC_CONF +0x44, MEM_CONF +0x48, DATE +0x80. |
| Interrupt sources | confirmed | `interrupts.h`: GPIO 16, GPIO_NMI 17, SPI2 20, SPI3 21, UART0 22, SDIO_HOST 24, LEDC 35, TWAI 36, USB(OTG) 37, RTC_CORE 38, RMT 39, I2C_EXT0 40, I2C_EXT1 41, USB_SERIAL_JTAG = ETS_MAX_INTR_SOURCE-1 (the last enumerator; compute from the header at build time). |

## Firmware behaviour

| Claim | Status | Evidence |
|---|---|---|
| Probe bit-bangs SCLK/MOSI/CS/DC at ~500 kHz; RST high 2 ms, low 1 ms (50 ms on the confirm pass), high, 30 ms; cmd+read = MOSI out, DC low, CS low, byte MSB-first (MOSI, SCLK high, SCLK low), DC high, MOSI INPUT_PULLUP, sample while SCLK low then pulse; CS high | confirmed | `XteinkDetect.cpp` `epdWriteByte`, `epdReadByte`, `epdCmdRead`, `runDisplayProbePass`. |
| Reads 1 byte with 0x71 (FLG) and 5 with 0x70 (VER); two passes; UC only if FLG not 0x00/0xFF with bit 0 set, VER non-uniform, passes agree | confirmed, incomplete | Also: when FLG is "driven" it reads **0xA2 (RMTP)**: 1 dummy byte + 48 MTP bytes, and can confirm a UC part from MTP alone (byte 0 == 0xA5 or a repeatable non-uniform dump). A UC model must answer 0xA2 too. After the probe every pin is set to INPUT (CS INPUT_PULLUP). |
| Real UC8179 returns VER 00 00 01 FF FF, FLG 0x13 | confirmed (doc) | Comment in `XteinkDetect.cpp` and the support doc. A UC8279 returns LUT_VER 0x02/0x68 in byte 2. |
| Probe verdict printed as `[XTDET]` | confirmed, gated | `Serial.printf("[%lu] [XTDET] bus probe VER=… FLG=… -> …")` only `if (Serial)`, i.e. only while HWCDC reports connected. |
| EpdBus uses `SPIClass` (SPI2/FSPI) without DMA; CS/DC via digitalWrite; frames via `SPI.writeBytes`; BUSY polled; refresh wait via GPIO CHANGE interrupt + semaphore | confirmed | `EpdBus.cpp` `cmd/data/cmdData`, `waitBusy` (poll with `busyIdle`), `waitRefreshComplete` (attachInterrupt CHANGE, 20 ms edge grace, 30 s timeout). Planes are sent as one CS-low burst of per-row `writeBytes` (`sendPlaneFlipped`) or one 48,000-byte `writeBytes` (`Ssd1677Driver::writeRam`). CrossPoint installs no slice hook (grep), so the ISR path is used for SSD1677; the UC X4 path (`BusyPolarity::UcIdleHigh`) always polls. |
| SSD1677 init order 0x12 + 10 ms + wait, 0x18 0x80, 0x0C booster, 0x01 ×3, 0x3C, 0x11, 0x44, 0x45, 0x4E, 0x4F, 0x46 0xF7 + wait, 0x47 0xF7 + wait | confirmed | `Ssd1677Driver::initController`, `setRamArea`. Booster `AE C7 C3 C0 80`, output control `DF 01 02`, border init 0x80, data entry 0x01 (X inc, Y dec), Y range written high-to-low. |
| Refresh 0x21 (0x00 fast / 0x40 else), optional 0x3C, optional 0x1A, 0x22 (0xF7 full, 0xFC fast, 0xD7 half), 0x20 | confirmed | `Ssd1677Driver::refresh` with `ssd1677DefaultConfig()`; border 0xC0 for all modes; half writes 0x1A 0x5A. Power-off: 0x3C 0x80, 0x22 0x03, 0x20, 200 ms. Deep sleep 0x10 0x03. Grayscale: 0x32 ×105, 0x03, 0x04 ×3, 0x2C, then 0x22 0xCC or 0xC7. |
| GT911 bring-up: GPIO2 low, 50 ms, reset dance (RST low, INT=level, 10 ms, RST high, 10 ms, 50 ms, INT input, 50 ms), probe 0x5D then 0x14, no config upload | confirmed | `InputManager::beginGt911`. The support doc quotes two different timings (2/8/60 ms and 10/10/50/50); the code is 10/10/50/50. |
| Polling: 16-bit reg MSB first, repeated start; 0x814E bit 7 ready, bit 4 Home, low nibble count; count×8 bytes at 0x8150 (coords at byte 0); write 0 to 0x814E | confirmed | `gt911ReadReg`, `pollGt911`, `gt911ClearStatus`. |
| `Serial.begin` with 250 ms delay and 1 ms TX timeout in dev builds; setup order (power rails, HAL, GPIO/power manager, wake cause, Right held = recovery, tilt, clock, SD; SD failure paints "SD card error") | confirmed | `src/main.cpp` `setup()`. Recovery key on X4 Pro is Down (GPIO7). "After USB power" wake does not sleep on X4 Pro. |
| Deep sleep via `esp_deep_sleep_start()` with ext1 wake on the power button | confirmed | `PowerManager.cpp`: `esp_sleep_enable_ext1_wakeup(mask, ANY_LOW)`, `gpio_deep_sleep_hold_en()`, `esp_deep_sleep_start()`. Rails are held at their off level (`gpio_hold_en`) through sleep. |
| ESP-IDF's connection monitor tick hook checks SOF every tick | confirmed | `usb_serial_jtag_connection_monitor.c` (fetched): reads `INT_RAW`, checks `SOF`, clears it, and after `USB_SERIAL_JTAG_CONN_MONITOR_TICKS` without SOF declares disconnect. |
| Community flashes CrossPoint onto the X4 Pro at 0x10000 only | confirmed | `firmware/README.md` "Flash an Xteink X4Pro…: `esptool.py --chip esp32s3 … write_flash 0x10000 firmware.bin`". The stock bootloader (IDF 6.0.1) and stock partition table stay. |

## Device first contact (D0)

| Item | Result |
|---|---|
| USB personality at idle | USB Serial/JTAG (0x303A:0x1001), serial string = MAC |
| `chip_id` with `--before default_reset` | connects first try; hard reset via RTS works |
| Left-button boot strap / power button semantics | **not testable**: no hands on the device this session |
| Double backup | `flash-2026-09-06-a.bin` and `-b.bin`, SHA-256 `90734554…143fe` identical |
| Boot log | `docs/device/boot-stock-7.2.4.log` (SSID/BSSID/IP redacted). Stock prints only IDF-level logs: no panel, touch, or probe lines. After `app_main` returns it starts WiFi at ~13 s and joins the saved network. |
| Console under stock | no `[XTDET]`/`[touch]` lines exist in stock firmware; those come from CrossPoint only |
| SD card | not imaged: stock exposes MSC only from its file-transfer screen, which needs touch input |
| Flash dump contents | NVS holds WiFi credentials (`user_config/sta_ssid`, `sta_pwd`, `wifi_creds`): the dump must never leave `images/` |

## Settled in M1

| Claim | Status | Evidence |
|---|---|---|
| Biggest unverified assumption: an Arduino-ESP32 3.3.x app on ESP-IDF 5.5 boots on QEMU's `esp32s3` machine with octal PSRAM | **confirmed** | The unmodified CrossPoint 1.6.0-x4pro image (IDF 5.5.5 bootloader at 0x0, Arduino core 3.3.11) boots on the stock `esp32s3` machine with `-m 8M` + `is_octal`: bootloader, PSRAM, `setup()` up to the RTC I2C probe and the SDMMC retries appear on UART0 within ~1.6 s guest time. No `unimp`/`guest_errors` hits apart from the M25P80 flash model rejecting SFDP commands and two reads at 0x10200C. |
| `-d unimp` shows every unmapped peripheral access | **corrected** | The SoC maps a silent catch-all `esp32s3.iomem` over 0x60000000..0x600d1000 at priority 0, and `esp32s3_soc_add_unimp_device` maps its unimp stubs at priority -1000 (below it). Nothing in that range is ever logged. The x4pro machine overlays named logging regions at priority 1 (`x4pro/<block>` lines in qemu.log). |
| Arduino `Wire` on core 3.x goes through the legacy `driver/i2c.h` path | **corrected** | Core 3.3.11 `Wire.cpp` calls `i2cWriteReadNonStop()` in `esp32-hal-i2c-ng.c`, which uses the new `driver/i2c_master.h` (`i2c_master_transmit_receive`). Observed on the console: `[E][esp32-hal-i2c-ng.c:372] i2cWriteReadNonStop(): i2c_master_transmit_receive failed: [259] ESP_ERR_INVALID_STATE`. |
| QMP `screendump` targets the panel | needs `device` | With `-display none` the default console is index 0 = the SoC's `esp_rgb` framebuffer (created by the parent machine). The panel device gets a qdev id (`epd`) so `screendump device=epd` selects its console. |
| Real UC8179/UC8279 answers `VER 00 00 01 FF FF`, `FLG 0x13` | **corrected for this unit** | The desk unit answers `VER=00 0F 68 00 00`, `FLG=13`, and a 48-byte MTP starting `A5 A5 1F 20 …` with LUT_VER 0x68 at 0x1A (CrossPoint on hardware, `docs/device/boot-crosspoint-1.6.0.log`). The SDK maps LUT_VER 0x68 to UC8279 800x480. |
| BUSY durations: full 1500 ms, fast 350 ms placeholders | **measured** | UC8279 on the device: PON 40 ms, GC/full DRF 1326 ms, DU/fast DRF 482–483 ms (CrossPoint `Wait complete` lines). |
| `-icount` on the two-CPU machine | **confirmed works** | `x4emu run --icount 3`: CrossPoint boots, probes, paints and idles with `[MEM]` lines every 10 s of guest time. |
| SdFat mounts a superfloppy FAT image | **corrected** | `SdFat/src/FatLib/FatPartition.cpp` `init(dev, part=1)`: with the default `part=1` it reads the MBR and fails when partition entry 1 has type 0; no superfloppy fallback. The device's card is MBR + one FAT32 partition; `tools/mksd.py` now writes the same layout. |
| Frontlight on LEDC channels 4 (cool) / 5 (warm) | **corrected for CrossPoint** | The board profile/stock use ch4/ch5; CrossPoint's FrontlightManager goes through the IDF `ledc` driver and lands on channels 0 (GPIO8) and 1 (GPIO9), timer 0, 25 kHz, 10-bit — observed in the emulator's GPIO matrix routing (`state.ledc`). |
| Deep-sleep hook: "hook the sleep-start write to stop the CPUs and schedule a reset with cause 5 and ext1 status" | **implemented** | Sequence confirmed from `rtc_sleep.c`: WAKEUP_STATE.WAKEUP_ENA, INT_CLR, STATE0.SLEEP_EN, spin on INT_RAW. Deep vs light distinguished by DIG_PWC.DG_WRAP_PD_EN. EXT1 trigger bit = BIT(1) (`soc/rtc.h`), RTC IO number of GPIO3 = 3 (`rtc_io_channel.h`). |
| Stock app needs an out-of-scope peripheral (USB OTG at boot) | **corrected** | It never touched USB OTG. It stalled in ROM `Cache_Occupy_Items` (D-cache occupy DONE never set by the cache model); with that overlaid it runs `app_main`, sleeps on a plain power-on by design (boot-preflight wants a ~600 ms power hold), boots after a modelled wake, probes the panel over SPI2 half-duplex, and idles after touching the SAR ADC and RTC IO (unmodelled). |
| Stock reads the panel ID with a bit-banged probe like the SDK | **corrected** | The stock app reads 0x70 through the SPI2 peripheral in half-duplex (5 dummy bytes with DC high); the panel device answers on that path as well. |
